# Issue Tracker Api

Simple issue tracker REST API created in Node.js.

### Development
* Install
    ```
    npm ci --legacy-peer-deps
    ```

* Start
    ```
    npm run start
    ```

### Future work
* Add tests
